# Ralph Context Snapshot

- task statement: Bring Offertio to enterprise-level completion using Ralph + Ultrawork, based on OFFERTIO_IMPLEMENTATION_SPEC and OFFERTIO_PRODUCT_LOGIC.
- desired outcome: Product logic gaps are implemented to a production-ready level, tests/build remain green, and the resulting app is coherent and launch-grade.
- known facts/evidence:
  - npm test currently passes with 176 tests.
  - npm run build currently passes.
  - Existing product logic already includes DACH rules, onboarding, document flow, PDF generation, Supabase persistence, customer-folder UI shell, and CSV export.
  - Biggest remaining gaps from code/spec comparison: persistent customer model, offer->invoice relation persistence/visibility, quiet customer reuse, inline profile-required data editing in document flow, preview model still step-based.
  - Project has Supabase migrations through 014 and no local .git repo.
- constraints:
  - No new dependencies unless necessary.
  - Keep diffs small, reviewable, and reversible.
  - Verify with tests/build/diagnostics.
  - Ralph planning gate is already satisfied by existing PRD/test-spec artifacts in .omx/plans.
- unknowns/open questions:
  - Exact current DB schema details beyond migrations for profiles/vorlagen/dokumente.
  - How much UI rework is needed to preserve current styling while upgrading preview and inline profile editing.
- likely codebase touchpoints:
  - src/app/(app)/dokument/neu/page.tsx
  - src/app/(app)/dokumente/page.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(app)/dokument/success/page.tsx
  - src/app/api/dokument/save/route.ts
  - src/lib/customer-folders.ts
  - src/lib/export.ts
  - src/lib/types.ts
  - supabase/migrations/*.sql
  - src/__tests__/* related tests